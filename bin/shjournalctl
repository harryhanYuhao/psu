#!/bin/sh

RED='\033[0;31m'
NC='\033[0m'
time=5

[ -z $1 ] || time=$1

[ -n $1 ] && echo -e "${RED}journalctl --since \"${time} minute ago\" | grep -C 3 kernel${NC}\n" && journalctl --since "${time} minute ago" | grep -C 3 kernel --color=always 

